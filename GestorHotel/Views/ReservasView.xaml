<Page x:Class="GestorHotel.Views.ReservasView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:GestorHotel.Views"
      mc:Ignorable="d" 
      d:DesignHeight="550" d:DesignWidth="950"
      Title="ReservasView">

    <Grid Background="#ECF0F1">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Grid.Row="0" Background="#E67E22" Padding="20">
            <TextBlock Text="Gestión de Reservas" 
                       FontSize="24" 
                       FontWeight="Bold" 
                       Foreground="White"/>
        </Border>

        <!-- Content -->
        <Grid Grid.Row="1" Margin="20">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- Search and Add Section -->
            <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,15">
                <TextBox x:Name="txtSearch" 
                         Width="300" 
                         Height="35" 
                         VerticalContentAlignment="Center"
                         Margin="0,0,10,0"
                         FontSize="14">
                    <TextBox.Style>
                        <Style TargetType="TextBox">
                            <Setter Property="Foreground" Value="Black"/>
                            <Style.Triggers>
                                <Trigger Property="Text" Value="">
                                    <Setter Property="Background">
                                        <Setter.Value>
                                            <VisualBrush Stretch="None" AlignmentX="Left">
                                                <VisualBrush.Visual>
                                                    <TextBlock Text="Buscar reserva..." 
                                                               Foreground="Gray" 
                                                               Margin="5,0,0,0"
                                                               FontSize="14"/>
                                                </VisualBrush.Visual>
                                            </VisualBrush>
                                        </Setter.Value>
                                    </Setter>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </TextBox.Style>
                </TextBox>
                <Button Content="Buscar" 
                        Width="100" 
                        Height="35" 
                        Background="#E67E22" 
                        Foreground="White" 
                        FontSize="14" 
                        Margin="0,0,10,0"
                        Cursor="Hand"/>
                <Button Content="+ Nueva Reserva" 
                        Width="150" 
                        Height="35" 
                        Background="#27AE60" 
                        Foreground="White" 
                        FontSize="14"
                        Cursor="Hand"/>
            </StackPanel>

            <!-- Reservations DataGrid -->
            <DataGrid Grid.Row="1" 
                      x:Name="dgReservas"
                      AutoGenerateColumns="False" 
                      CanUserAddRows="False"
                      CanUserDeleteRows="False"
                      IsReadOnly="True"
                      Background="White"
                      AlternatingRowBackground="#F8F9FA"
                      GridLinesVisibility="None"
                      HeadersVisibility="Column"
                      RowHeight="40">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="ID Reserva" Binding="{Binding Id}" Width="100"/>
                    <DataGridTextColumn Header="Huésped" Binding="{Binding Huesped}" Width="150"/>
                    <DataGridTextColumn Header="Habitación" Binding="{Binding Habitacion}" Width="100"/>
                    <DataGridTextColumn Header="Check-In" Binding="{Binding CheckIn}" Width="120"/>
                    <DataGridTextColumn Header="Check-Out" Binding="{Binding CheckOut}" Width="120"/>
                    <DataGridTextColumn Header="Noches" Binding="{Binding Noches}" Width="80"/>
                    <DataGridTextColumn Header="Total" Binding="{Binding Total}" Width="100"/>
                    <DataGridTextColumn Header="Estado" Binding="{Binding Estado}" Width="100"/>
                    <DataGridTemplateColumn Header="Acciones" Width="*">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal">
                                    <Button Content="Ver" 
                                            Width="70" 
                                            Height="30" 
                                            Background="#E67E22" 
                                            Foreground="White" 
                                            Margin="5,0"
                                            Cursor="Hand"/>
                                    <Button Content="Editar" 
                                            Width="70" 
                                            Height="30" 
                                            Background="#F39C12" 
                                            Foreground="White" 
                                            Margin="5,0"
                                            Cursor="Hand"/>
                                    <Button Content="Cancelar" 
                                            Width="70" 
                                            Height="30" 
                                            Background="#E74C3C" 
                                            Foreground="White" 
                                            Margin="5,0"
                                            Cursor="Hand"/>
                                </StackPanel>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                </DataGrid.Columns>
            </DataGrid>
        </Grid>
    </Grid>
</Page>
