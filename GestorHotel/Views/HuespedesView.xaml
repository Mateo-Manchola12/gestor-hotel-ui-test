<Page x:Class="GestorHotel.Views.HuespedesView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:GestorHotel.Views"
      mc:Ignorable="d" 
      d:DesignHeight="550" d:DesignWidth="950"
      Title="HuespedesView">

    <Grid Background="#ECF0F1">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Grid.Row="0" Background="#9B59B6" Padding="20">
            <TextBlock Text="Gestión de Huéspedes" 
                       FontSize="24" 
                       FontWeight="Bold" 
                       Foreground="White"/>
        </Border>

        <!-- Content -->
        <Grid Grid.Row="1" Margin="20">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- Search and Add Section -->
            <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,15">
                <TextBox x:Name="txtSearch" 
                         Width="300" 
                         Height="35" 
                         VerticalContentAlignment="Center"
                         Margin="0,0,10,0"
                         FontSize="14">
                    <TextBox.Style>
                        <Style TargetType="TextBox">
                            <Setter Property="Foreground" Value="Black"/>
                            <Style.Triggers>
                                <Trigger Property="Text" Value="">
                                    <Setter Property="Background">
                                        <Setter.Value>
                                            <VisualBrush Stretch="None" AlignmentX="Left">
                                                <VisualBrush.Visual>
                                                    <TextBlock Text="Buscar huésped..." 
                                                               Foreground="Gray" 
                                                               Margin="5,0,0,0"
                                                               FontSize="14"/>
                                                </VisualBrush.Visual>
                                            </VisualBrush>
                                        </Setter.Value>
                                    </Setter>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </TextBox.Style>
                </TextBox>
                <Button Content="Buscar" 
                        Width="100" 
                        Height="35" 
                        Background="#9B59B6" 
                        Foreground="White" 
                        FontSize="14" 
                        Margin="0,0,10,0"
                        Cursor="Hand"/>
                <Button Content="+ Nuevo Huésped" 
                        Width="150" 
                        Height="35" 
                        Background="#27AE60" 
                        Foreground="White" 
                        FontSize="14"
                        Cursor="Hand"/>
            </StackPanel>

            <!-- Guests DataGrid -->
            <DataGrid Grid.Row="1" 
                      x:Name="dgHuespedes"
                      AutoGenerateColumns="False" 
                      CanUserAddRows="False"
                      CanUserDeleteRows="False"
                      IsReadOnly="True"
                      Background="White"
                      AlternatingRowBackground="#F8F9FA"
                      GridLinesVisibility="None"
                      HeadersVisibility="Column"
                      RowHeight="40">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="80"/>
                    <DataGridTextColumn Header="Nombre Completo" Binding="{Binding Nombre}" Width="200"/>
                    <DataGridTextColumn Header="Documento" Binding="{Binding Documento}" Width="120"/>
                    <DataGridTextColumn Header="Email" Binding="{Binding Email}" Width="200"/>
                    <DataGridTextColumn Header="Teléfono" Binding="{Binding Telefono}" Width="120"/>
                    <DataGridTextColumn Header="País" Binding="{Binding Pais}" Width="100"/>
                    <DataGridTemplateColumn Header="Acciones" Width="*">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal">
                                    <Button Content="Ver" 
                                            Width="70" 
                                            Height="30" 
                                            Background="#9B59B6" 
                                            Foreground="White" 
                                            Margin="5,0"
                                            Cursor="Hand"/>
                                    <Button Content="Editar" 
                                            Width="70" 
                                            Height="30" 
                                            Background="#F39C12" 
                                            Foreground="White" 
                                            Margin="5,0"
                                            Cursor="Hand"/>
                                    <Button Content="Eliminar" 
                                            Width="70" 
                                            Height="30" 
                                            Background="#E74C3C" 
                                            Foreground="White" 
                                            Margin="5,0"
                                            Cursor="Hand"/>
                                </StackPanel>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                </DataGrid.Columns>
            </DataGrid>
        </Grid>
    </Grid>
</Page>
