<Page x:Class="GestorHotel.Views.ResenasView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:GestorHotel.Views"
      mc:Ignorable="d" 
      d:DesignHeight="550" d:DesignWidth="950"
      Title="ResenasView">

    <Grid Background="#ECF0F1">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Grid.Row="0" Background="#16A085" Padding="20">
            <TextBlock Text="Gestión de Reseñas" 
                       FontSize="24" 
                       FontWeight="Bold" 
                       Foreground="White"/>
        </Border>

        <!-- Content -->
        <Grid Grid.Row="1" Margin="20">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- Search and Add Section -->
            <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,15">
                <TextBox x:Name="txtSearch" 
                         Width="300" 
                         Height="35" 
                         VerticalContentAlignment="Center"
                         Margin="0,0,10,0"
                         FontSize="14">
                    <TextBox.Style>
                        <Style TargetType="TextBox">
                            <Setter Property="Foreground" Value="Black"/>
                            <Style.Triggers>
                                <Trigger Property="Text" Value="">
                                    <Setter Property="Background">
                                        <Setter.Value>
                                            <VisualBrush Stretch="None" AlignmentX="Left">
                                                <VisualBrush.Visual>
                                                    <TextBlock Text="Buscar reseña..." 
                                                               Foreground="Gray" 
                                                               Margin="5,0,0,0"
                                                               FontSize="14"/>
                                                </VisualBrush.Visual>
                                            </VisualBrush>
                                        </Setter.Value>
                                    </Setter>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </TextBox.Style>
                </TextBox>
                <Button Content="Buscar" 
                        Width="100" 
                        Height="35" 
                        Background="#16A085" 
                        Foreground="White" 
                        FontSize="14" 
                        Margin="0,0,10,0"
                        Cursor="Hand"/>
                <ComboBox Width="150" 
                          Height="35" 
                          Margin="0,0,10,0"
                          VerticalContentAlignment="Center"
                          FontSize="14">
                    <ComboBoxItem Content="Todas las estrellas" IsSelected="True"/>
                    <ComboBoxItem Content="5 estrellas"/>
                    <ComboBoxItem Content="4 estrellas"/>
                    <ComboBoxItem Content="3 estrellas"/>
                    <ComboBoxItem Content="2 estrellas"/>
                    <ComboBoxItem Content="1 estrella"/>
                </ComboBox>
            </StackPanel>

            <!-- Reviews DataGrid -->
            <DataGrid Grid.Row="1" 
                      x:Name="dgResenas"
                      AutoGenerateColumns="False" 
                      CanUserAddRows="False"
                      CanUserDeleteRows="False"
                      IsReadOnly="True"
                      Background="White"
                      AlternatingRowBackground="#F8F9FA"
                      GridLinesVisibility="None"
                      HeadersVisibility="Column"
                      RowHeight="40">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="80"/>
                    <DataGridTextColumn Header="Huésped" Binding="{Binding Huesped}" Width="150"/>
                    <DataGridTextColumn Header="Habitación" Binding="{Binding Habitacion}" Width="100"/>
                    <DataGridTextColumn Header="Calificación" Binding="{Binding Calificacion}" Width="100"/>
                    <DataGridTextColumn Header="Fecha" Binding="{Binding Fecha}" Width="120"/>
                    <DataGridTextColumn Header="Comentario" Binding="{Binding Comentario}" Width="*"/>
                    <DataGridTemplateColumn Header="Acciones" Width="200">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal">
                                    <Button Content="Ver Detalle" 
                                            Width="90" 
                                            Height="30" 
                                            Background="#16A085" 
                                            Foreground="White" 
                                            Margin="5,0"
                                            Cursor="Hand"/>
                                    <Button Content="Eliminar" 
                                            Width="70" 
                                            Height="30" 
                                            Background="#E74C3C" 
                                            Foreground="White" 
                                            Margin="5,0"
                                            Cursor="Hand"/>
                                </StackPanel>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                </DataGrid.Columns>
            </DataGrid>
        </Grid>
    </Grid>
</Page>
